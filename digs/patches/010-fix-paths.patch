--- a/src/cli.rs
+++ b/src/cli.rs
@@ -20,7 +20,7 @@ pub struct Opts {
     pub rtype: RecordType,
 
     /// Specify an alternate configuration file
-    #[arg(short, long, default_value_os_t = PathBuf::from("digs.toml"))]
+    #[arg(short, long, default_value_os_t = PathBuf::from("/opt/etc/digs.toml"))]
     pub config: PathBuf,
 }
 
