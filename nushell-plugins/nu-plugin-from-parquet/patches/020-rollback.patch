--- a/src/main.rs
+++ b/src/main.rs
@@ -46,7 +46,7 @@ impl Plugin for FromParquet {
     fn run(
         &mut self,
         name: &str,
-        _config: &Option<Value>,
+//        _config: &Option<Value>,
         call: &EvaluatedCall,
         input: &Value,
     ) -> Result<Value, LabeledError> {
