--- a/Cargo.toml
+++ b/Cargo.toml
@@ -17,3 +17,14 @@ crossterm = "0.27.0"
 chrono = "0.4"
 clap = { version = "4.3.1", features = ["derive"] }
 ratatui = "0.24.0"
+
+[dev-dependencies]
+# error[E0635]: unknown feature `stdsimd`
+ahash = "0.8.11"
+
+[patch.crates-io]
+# localtime/zoneinfo
+chrono = { path = "../chrono-0.4.42" }
+iana-time-zone = { path = "../iana-time-zone-0.1.64" }
+# all: no SYS_getrandom
+getrandom = { path = "../getrandom-0.3.4" }
