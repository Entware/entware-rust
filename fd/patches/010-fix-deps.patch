--- a/Cargo.toml
+++ b/Cargo.toml
@@ -93,3 +93,7 @@ use-jemalloc = ["tikv-jemallocator"]
 completions = ["clap_complete"]
 base = ["use-jemalloc"]
 default = ["use-jemalloc", "completions"]
+
+[patch.crates-io]
+# all: no SYS_getrandom
+getrandom = { path = "../getrandom-0.3.3" }
