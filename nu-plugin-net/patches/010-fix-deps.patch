--- a/Cargo.toml
+++ b/Cargo.toml
@@ -16,8 +16,8 @@ name = "nu_plugin_net"
 [lib]
 
 [dependencies]
-nu-plugin = "0.104.0"
-nu-protocol = { version = "0.104.0", features = ["plugin"] }
+nu-plugin = "0.106.1"
+nu-protocol = { version = "0.106.1", features = ["plugin"] }
 pnet = "0.35.0"
 
 # The profile that 'cargo dist' will build with
@@ -41,3 +41,12 @@ install-path = "CARGO_HOME"
 pr-run-mode = "plan"
 # Whether to install an updater program
 install-updater = false
+
+[patch.crates-io]
+nu-plugin = { path = "../nushell-0.106.1/crates/nu-plugin" }
+nu-protocol = { path = "../nushell-0.106.1/crates/nu-protocol" }
+# localtime/zoneinfo
+chrono = { path = "../chrono-0.4.40" }
+iana-time-zone = { path = "../iana-time-zone-0.1.61" }
+# all: no SYS_getrandom
+getrandom = { path = "../getrandom-0.2.16" }
