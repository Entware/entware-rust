--- a/Cargo.toml
+++ b/Cargo.toml
@@ -40,3 +40,10 @@ panic = "abort"
 [profile.dist]
 inherits = "release"
 lto = "thin"
+
+[patch.crates-io]
+# localtime/zoneinfo
+chrono = { path = "../chrono-0.4.42" }
+iana-time-zone = { path = "../iana-time-zone-0.1.64" }
+# all: no SYS_getrandom
+getrandom = { path = "../getrandom-0.3.1" }
