--- a/Cargo.toml
+++ b/Cargo.toml
@@ -532,3 +532,8 @@ inherits = "release"
 opt-level = "z"
 panic = "abort"
 strip = true
+
+[patch.crates-io]
+# localtime/zoneinfo
+chrono = { path = "../../chrono-0.4.33" }
+iana-time-zone = { path = "../../iana-time-zone-0.1.59" }
