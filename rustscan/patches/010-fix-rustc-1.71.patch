# error: to use a constant of type `TypeId` in a pattern, `TypeId` must be annotated with `#[derive(PartialEq, Eq)]`
# error: could not compile `value-bag` (lib) due to 3 previous errors

--- a/Cargo.toml
+++ b/Cargo.toml
@@ -39,6 +39,7 @@ trust-dns-resolver = { version = "0.19.5
 anyhow = "1.0.40"
 subprocess = "0.2.6"
 text_placeholder = { version = "0.4", features = ["struct_context"] }
+value-bag = "1.4.1"
 
 [dev-dependencies]
 wait-timeout = "0.2"
