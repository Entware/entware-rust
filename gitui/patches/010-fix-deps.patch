--- a/Cargo.toml
+++ b/Cargo.toml
@@ -81,3 +81,8 @@ codegen-units = 1
 # makes their debug profile slow
 [profile.dev.package."ratatui"]
 opt-level = 3
+
+[patch.crates-io]
+# localtime/zoneinfo
+chrono = { path = "../chrono-0.4.31" }
+iana-time-zone = { path = "../iana-time-zone-0.1.59" }
