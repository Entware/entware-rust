--- a/Cargo.toml
+++ b/Cargo.toml
@@ -90,5 +90,5 @@ panic = 'abort'
 strip = true
 
 [features]
-default = ["vendored-lua"]
-vendored-lua = ["mlua/vendored"]
+#default = ["vendored-lua"]
+#vendored-lua = ["mlua/vendored"]
