--- a/Cargo.toml
+++ b/Cargo.toml
@@ -73,3 +73,12 @@ required-features = ["cli"]
 name = "monolith-gui"
 path = "src/gui.rs"
 required-features = ["gui"]
+
+[patch.crates-io]
+# localtime/zoneinfo
+chrono = { path = "../chrono-0.4.40" }
+iana-time-zone = { path = "../iana-time-zone-0.1.62" }
+# all: no SYS_getrandom
+getrandom = { path = "../getrandom-0.3.2" }
+# openssl
+openssl-probe = { path = "../openssl-probe-0.1.6" }
