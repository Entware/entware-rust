--- a/Cargo.toml
+++ b/Cargo.toml
@@ -62,3 +62,10 @@ if_not_else = "warn"
 manual_let_else = "warn"
 # Disable for now, we have a few occurrences
 # panic = "warn"
+
+[patch.crates-io]
+# localtime/zoneinfo
+chrono = { path = "../chrono-0.4.40" }
+iana-time-zone = { path = "../iana-time-zone-0.1.62" }
+# all: no SYS_getrandom
+getrandom = { path = "../getrandom-0.3.1" }
